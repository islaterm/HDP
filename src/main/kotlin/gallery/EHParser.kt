package gallery

class EHParser {
    @Suppress("UNUSED_PARAMETER")
    fun parse(url: String): EHGallery {
        TODO("Hacer request a la API, parsear y agregar a data class (podría ser un builder)")
    }

    @Suppress("UNUSED_PARAMETER")
    fun parse(urls: List<String>): EHGallery {
        TODO("Parsear de a 25 y esperar")
    }
}
